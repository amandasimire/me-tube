import React from 'react';
import { AiFillHome, AiOutlinePlaySquare } from 'react-icons/ai';

const Sidebar = () => {
  const menuItems = [
    { name: 'Home', icon: <AiFillHome /> },
    { name: 'Trending', icon: <AiOutlinePlaySquare /> },
    { name: 'Subscriptions', icon: <AiOutlinePlaySquare /> },
    { name: 'Library', icon: <AiOutlinePlaySquare /> },
  ];

  return (
    <div className="bg-white h-full p-4 w-64 border-r">
      {menuItems.map((item, index) => (
        <div
          key={index}
          className="flex items-center space-x-4 p-2 hover:bg-gray-200 cursor-pointer"
        >
          {item.icon}
          <span>{item.name}</span>
        </div>
      ))}
    </div>
  );
};

export default Sidebar;
